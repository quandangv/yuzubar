image: untested build

build:
  stage: .pre
    - git clone https://github.com/quandangv/linked_nodes 
    - cd linked_nodes
    - ./build.sh -A
  script:
    - make
    - make install
  #artifacts:
  #  paths:

