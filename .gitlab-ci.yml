image: untested build

build:
  stage: build
  before_script:
    - git clone https://github.com/quandangv/linked_nodes 
    - cd linked_nodes
    - ./build.sh -A
  script:
    - make
    - make install
  #artifacts:
  #  paths:

